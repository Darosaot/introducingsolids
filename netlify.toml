[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback so client-side routing works on deep links / refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
